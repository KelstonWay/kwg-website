VITE_SUPABASE_URL=https://wuemcpptmjmvtzaciezq.supabase.co
VITE_SUPABASE_ANON_KEY=
RESEND_API_KEY=
# Where buyer mail and order alerts land. orders@ is a shared mailbox so the whole
# team sees it. Server only — never expose this to the browser.
TEAM_EMAIL=orders@kelstonway.com
